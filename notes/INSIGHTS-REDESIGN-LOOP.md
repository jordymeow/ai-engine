# Insights screen redesign loop

Started 2026-09-15 at Jordy's request, after the Discussions redesign (see notes/DISCUSSIONS-REDESIGN-LOOP.md): bring the same care to AI Engine → Insights. Loop every 15 minutes (cron b7f5936e) for about 2 hours, Discussions and Insights together. Never commit.

Screen: AI Engine → Insights on ai.nekod.net. Code: `app/js/screens/queries/` (Insights.js, Queries.js, ExportModal.js). Build with `pnpm build`.

## What must keep working
- Query Logs and MCP Logs: filters, sort, paging, refresh, export, selecting a row, the log details
- Users, Guests and System limits: reading and saving them
- No em-dashes in UI copy, no console errors

## Checklist
- [x] Words and IDs never break mid-letter; no cell wraps into 3+ lines
- [x] Query Logs row readable at a glance: when, who, where it came from, which model, how much
- [x] Session IDs, hashes and raw values out of the way (tooltip or details)
- [x] MCP Logs as readable as Query Logs, consistent look
- [x] Log details (selected row) tidy, like the Discussions Information block (Query Logs; MCP Arguments and Result stay JSON on purpose)
- [x] Limits views clear and consistent (Users, Guests, System render; no change needed)
- [x] Works at narrow widths
- [x] Regression pass: filters, sort, refresh, export, delete, selection, limits sections (saving not exercised on purpose, to keep the site's limits unchanged)

## Iterations

| # | Time | What I looked at | What I changed | Result |
|---|------|------------------|----------------|--------|
| 1 | 17:05 | Query Logs: Time and User on two lines, Scope showed a session hash wrapping over three lines ("N/A" when missing), Price shown twice ($0.0000 over $0.000109) plus a separate accuracy dot column, Tokens over a "tokens" line, header row repeated under the table. MCP Logs: Client over "shared secret", Duration over "ms", em-dash placeholders | Extracted the Discussions building blocks into `app/js/components/TableCells.js` (StyledCell, shortTime, parseStoredDate, plus new UserCell and formatPrice), used by both screens. Query Logs: short time with full date on hover, one-line user, readable scope label (Models API, Chatbot, Not set) with the session in the tooltip, model name over environment, tokens as one formatted number, one precise price with the accuracy dot inline (exact value and accuracy explanation in the tooltip), tiny prices read "<$0.000001". MCP Logs: client icon and name on one line (auth detail in the tooltip), tool in monospace, rounded status pill (Success, Denied, Error), "10 ms". Both tables: minmax columns, compact variant (no repeated header), overflow-x container. Removed the unused formatter imports | Both logs are single-glance rows, no console errors, Discussions still renders with the shared UserCell. Two bugs found and fixed during the pass: empty-string prices and 8-decimal prices (embeddings) hid the price text; the client icon rendered on its own line. Next: log details on row selection, limits views, filters and paging regression, narrow width |
| 2 | 17:25 | Selecting a Query Log row: the sidebar showed Query, Reply, Fields and Stats tabs that were only raw JSON trees (message, instructions, model settings, class names, session, usage). Activity chart and Limits block looked fine | New first tab "Details": the exchange as chat bubbles (user message, assistant reply), Instructions clamped with Show all, then label/value rows for Model, Environment (name, not ID), Feature, Max tokens, Temperature, Scope, Session (monospace, copy on click), Tokens in, Tokens out, Total tokens, Price. The raw Query/Reply/Fields/Stats tabs stay after it. Moved InfoRow and ContextText (now with a label prop) from Discussions.js into TableCells.js with a new ChatBubble, so both screens share them. MCP Arguments and Result kept as JSON: they are real tool payloads. Rewrote 4 old em-dashes in Insights.js comments | Details tab opens by default and reads clearly, Discussions Information block still renders from the shared component. Next: MCP Stats tab and Top Tools, regression (filters, paging, sort, export, delete, limits saving), narrow width |
| 3 | 17:50 | MCP Logs row selection: Arguments tab fine (small JSON), but Result showed the whole JSON-RPC envelope (jsonrpc, id, result.content[].text) and Stats was raw JSON (client_name, auth_method, duration_ms, status). Then a regression pass on Insights | New first tab "Details" for MCP calls: Tool (monospace), Status, Error when present, Duration, Client (Bearer token, OAuth with the app name, or WordPress session), the arguments as label/value rows, and the tool's answer pulled out of the envelope (result.content text, or the error message) in a bubble labelled "Result" (ChatBubble got a label prop). When arguments and results are not captured, a hint points to Settings → MCP → Logging. Raw Arguments, Result and Stats tabs kept | Details reads clearly for wp_delete_post (ID 534, force true, "Post #534 deleted"). Regression: Tokens sort puts the largest queries first (637,130), Export opens "Export Query Logs", Delete All opens its "This cannot be undone" confirmation and Escape closes it without deleting, Limits Users/Guests/System sections switch and render (System: 20 dollars per month), no console errors. Next: narrow width for Insights, then a fresh look at both screens |
| 4 | 18:05 | Narrow width (#wpbody-content capped at 820px, the window resize does not change the page width) and a fresh look at the sidebar | Nothing changed. Query Logs columns sit at their minimums (82, 72, 90, 160, 84, 108px), the table's right edge (672px) stays inside its card (691px) and scrolls sideways for Tokens and Price, like Discussions. Activity chart and Limits block wrap cleanly in the narrower sidebar. The Information legend still matches the accuracy dots | CLEAN PASS (1 of 2 for stopping). No console errors. Next pass: a final fresh look at both Insights and Discussions |
| 5 | 18:20 | Final look at both screens: Insights Query Logs with a selected row and its Details tab, Discussions with a selected discussion | Nothing changed. Both screens share the same look (one-line cells, pills, short times, chat bubbles, label/value rows), no console errors | CLEAN PASS (2 of 2): loop stopped (cron b7f5936e deleted). Nothing committed, awaiting Jordy's review |
